
function toggleCardText(card) {
    card.classList.toggle('show-text');
  }

/* document.querySelectorAll('.card').forEach(card => {
    card.addEventListener('click', function() {
        this.querySelector('.card-img').classList.toggle('opaque');
        this.querySelector('.card-img-overlay').classList.toggle('show');
    });
});
 */